{
  "name": "basic-host-remote",
  "private": true,
  "workspaces": [
    "./host",
    "./shared",
    "./app-tokens"
  ],
  "version": "0.0.0",
  "scripts": {
    "start": "lerna run --scope=basic-host-remote* --parallel start",
    "build": "lerna run --scope=basic-host-remote* --parallel build",
    "serve": "lerna run --scope=basic-host-remote* --parallel serve",
    "clean": "lerna run --scope=basic-host-remote* --parallel clean"
  },
  "devDependencies": {
    "lerna": "^6.5.1",
    "wait-on": "7.0.1"
  }
}
